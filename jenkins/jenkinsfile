pipeline {
    agent any

    stages {
        stage('Unit Tests - Backend') {
            steps {
                dir('bug-tracker/bugtracker-backend') {
                    bat 'go test -v ./...'
                }
            }
        }
    }
}