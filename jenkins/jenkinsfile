pipeline {
    agent any

    stages {
        stage('Unit Tests - Backend') {
            steps {
                dir('bug-tracker/bugtracker-backend') {
                    // This is the correct Microsoft command to run all unit tests in Visual Studio
                    bat 'dotnet test'
                }
            }
        }
    }
}